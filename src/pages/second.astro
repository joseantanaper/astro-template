---
import Layout from '../layouts/Layout.astro'
---

<Layout title="Second" />
<script>
  document.addEventListener(
    'astro:page-load',
    () => {
      console.log('second', 'astro:page-load', 'once')
    },
    { once: true }
  )
  document.addEventListener('astro:page-load', () => {
    console.log('second', 'astro:page-load')
  })
  document.addEventListener('astro:after-swap', () => {
    console.log('second', 'astro:after-swap')
  })
</script>
